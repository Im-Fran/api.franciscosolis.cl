{
  "name": "auth",
  "type": "module",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0 --port 8789 --inspector-port 9231",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "typecheck": "tsc --noEmit -p test/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply franciscosolis_auth --local",
    "db:migrate:remote": "wrangler d1 migrations apply franciscosolis_auth --remote",
    "keys:generate": "node scripts/generate-keys.mjs",
    "admin:bootstrap": "node scripts/bootstrap-admin.mjs"
  },
  "dependencies": {
    "@hono/standard-validator": "catalog:",
    "@valibot/to-json-schema": "catalog:",
    "axios": "catalog:",
    "drizzle-orm": "catalog:",
    "hono": "catalog:",
    "hono-openapi": "catalog:",
    "valibot": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@cloudflare/workers-types": "^5.20260730.1",
    "@vitest/coverage-istanbul": "catalog:",
    "drizzle-kit": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
