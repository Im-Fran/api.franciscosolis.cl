{
  "name": "pages",
  "type": "module",
  "license": "GPL-3.0-only",
  "version": "0.3.0",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0 --port 8792 --inspector-port 9233",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "typecheck": "tsc --noEmit -p test/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "db:generate": "drizzle-kit generate",
    "db:migrate:list": "wrangler d1 migrations list franciscosolis_pages --remote",
    "db:migrate:local": "wrangler d1 migrations apply franciscosolis_pages --local",
    "db:migrate:remote": "wrangler d1 migrations apply franciscosolis_pages --remote"
  },
  "dependencies": {
    "@hono/standard-validator": "catalog:",
    "@valibot/to-json-schema": "catalog:",
    "hono": "catalog:",
    "hono-openapi": "catalog:",
    "drizzle-orm": "catalog:",
    "valibot": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@vitest/coverage-istanbul": "catalog:",
    "drizzle-kit": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
